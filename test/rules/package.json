{
  "name": "shipeast-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Firestore security rules test suite (P2-01). Runs against the emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project shipeast-rules-test 'node --test'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "firebase-tools": "^13.20.2"
  }
}
