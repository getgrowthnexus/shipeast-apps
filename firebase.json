{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "runtime": "nodejs20",
      "predeploy": [
        "npm --prefix \"$RESOURCE_DIR\" run build"
      ],
      "ignore": [
        "node_modules",
        ".git",
        "*.local"
      ]
    }
  ],
  "_comment_hosting": [
    "Hosting is deliberately NOT configured here. admin_panel/firebase.json owns it and its",
    "deploy works today; declaring hosting in two places would give the panel two sources of",
    "truth for what gets published. This file owns the backend: rules, indexes, functions.",
    "Deploy backend from the repo root, the panel from admin_panel/."
  ],
  "emulators": {
    "firestore": { "port": 8080 },
    "auth": { "port": 9099 },
    "storage": { "port": 9199 },
    "functions": { "port": 5001 },
    "ui": { "enabled": true }
  }
}
